<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Conscious Living - Philosophy for Daily Life</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        :root {
            /* Earth & Body */
            --terracotta: #C36A2D;
            --sand: #E1C699;
            --olive: #6B705C;
            --soil: #4B3B2B;
            
            /* Water & Flow */
            --deep-sea: #264653;
            --teal: #2A9D8F;
            --mist: #B5E2E0;
            --silver: #CED4DA;
            
            /* Sun & Sky */
            --gold: #F4A261;
            --coral: #E76F51;
            --pink: #F28482;
            --sky: #87CEEB;
            
            /* Cosmos & Insight */
            --star-black: #0B0C10;
            --violet: #5E548E;
            --nebula: #6A4C93;
            --starlight: #F7F7F7;
        }

        body {
            font-family: 'Georgia', serif;
            background: linear-gradient(135deg, var(--sand) 0%, var(--mist) 50%, var(--sky) 100%);
            min-height: 100vh;
            color: var(--soil);
            overflow-x: hidden;
        }

        /* Floating orbs animation */
        .cosmic-orb {
            position: fixed;
            border-radius: 50%;
            opacity: 0.3;
            filter: blur(40px);
            animation: float 20s infinite ease-in-out;
            pointer-events: none;
            z-index: 0;
        }

        .orb1 {
            width: 300px;
            height: 300px;
            background: var(--violet);
            top: -150px;
            left: -150px;
            animation-delay: 0s;
        }

        .orb2 {
            width: 400px;
            height: 400px;
            background: var(--teal);
            bottom: -200px;
            right: -200px;
            animation-delay: 5s;
        }

        .orb3 {
            width: 250px;
            height: 250px;
            background: var(--gold);
            top: 50%;
            left: 50%;
            animation-delay: 10s;
        }

        @keyframes float {
            0%, 100% { transform: translate(0, 0) scale(1); }
            25% { transform: translate(100px, -50px) scale(1.1); }
            50% { transform: translate(-50px, 100px) scale(0.9); }
            75% { transform: translate(-100px, -30px) scale(1.05); }
        }

        /* Navigation */
        nav {
            position: fixed;
            top: 0;
            width: 100%;
            padding: 20px 50px;
            background: rgba(247, 247, 247, 0.1);
            backdrop-filter: blur(10px);
            z-index: 1000;
            transition: all 0.3s ease;
        }

        nav.scrolled {
            background: rgba(247, 247, 247, 0.95);
            box-shadow: 0 2px 20px rgba(0,0,0,0.1);
        }

        .nav-container {
            max-width: 1400px;
            margin: 0 auto;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .logo {
            font-size: 24px;
            font-weight: bold;
            background: linear-gradient(135deg, var(--terracotta), var(--violet));
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }

        .nav-links {
            display: flex;
            gap: 30px;
            list-style: none;
        }

        .nav-links a {
            color: var(--soil);
            text-decoration: none;
            transition: all 0.3s ease;
            position: relative;
        }

        .nav-links a::after {
            content: '';
            position: absolute;
            bottom: -5px;
            left: 0;
            width: 0;
            height: 2px;
            background: var(--coral);
            transition: width 0.3s ease;
        }

        .nav-links a:hover::after {
            width: 100%;
        }

        /* Hero Section */
        .hero {
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            position: relative;
            z-index: 1;
        }

        .hero-content {
            text-align: center;
            max-width: 900px;
            padding: 0 20px;
            animation: fadeInUp 1s ease;
        }

        @keyframes fadeInUp {
            from {
                opacity: 0;
                transform: translateY(30px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        h1 {
            font-size: clamp(3rem, 8vw, 5rem);
            margin-bottom: 20px;
            background: linear-gradient(135deg, var(--deep-sea), var(--nebula), var(--terracotta));
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            line-height: 1.1;
        }

        .tagline {
            font-size: 1.5rem;
            color: var(--olive);
            margin-bottom: 40px;
            font-style: italic;
        }

        .cta-button {
            display: inline-block;
            padding: 15px 40px;
            background: linear-gradient(135deg, var(--coral), var(--gold));
            color: white;
            text-decoration: none;
            border-radius: 30px;
            font-size: 1.1rem;
            transition: all 0.3s ease;
            box-shadow: 0 5px 20px rgba(231, 111, 81, 0.3);
        }

        .cta-button:hover {
            transform: translateY(-3px);
            box-shadow: 0 8px 30px rgba(231, 111, 81, 0.4);
        }

        /* Philosophy Cards Section */
        .philosophy-section {
            padding: 100px 50px;
            max-width: 1400px;
            margin: 0 auto;
            position: relative;
            z-index: 1;
        }

        .section-title {
            text-align: center;
            font-size: 3rem;
            margin-bottom: 20px;
            color: var(--deep-sea);
        }

        .section-subtitle {
            text-align: center;
            font-size: 1.2rem;
            color: var(--olive);
            margin-bottom: 60px;
            max-width: 700px;
            margin-left: auto;
            margin-right: auto;
        }

        .philosophy-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(350px, 1fr));
            gap: 40px;
        }

        .philosophy-card {
            background: rgba(255, 255, 255, 0.7);
            backdrop-filter: blur(10px);
            border-radius: 20px;
            padding: 40px;
            transition: all 0.3s ease;
            border: 1px solid rgba(107, 112, 92, 0.2);
            position: relative;
            overflow: hidden;
        }

        .philosophy-card::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            height: 4px;
            background: linear-gradient(90deg, var(--teal), var(--violet));
            transform: scaleX(0);
            transition: transform 0.3s ease;
        }

        .philosophy-card:hover::before {
            transform: scaleX(1);
        }

        .philosophy-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 20px 40px rgba(0,0,0,0.1);
        }

        .card-icon {
            width: 60px;
            height: 60px;
            margin-bottom: 20px;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 24px;
        }

        .icon-presence { background: linear-gradient(135deg, var(--teal), var(--mist)); }
        .icon-flow { background: linear-gradient(135deg, var(--gold), var(--coral)); }
        .icon-connection { background: linear-gradient(135deg, var(--violet), var(--nebula)); }

        .card-title {
            font-size: 1.8rem;
            margin-bottom: 15px;
            color: var(--deep-sea);
        }

        .card-philosophy {
            font-style: italic;
            color: var(--violet);
            margin-bottom: 20px;
            padding: 15px;
            background: rgba(94, 84, 142, 0.05);
            border-left: 3px solid var(--violet);
            border-radius: 5px;
        }

        .card-practice {
            color: var(--soil);
            line-height: 1.6;
            margin-bottom: 20px;
        }

        .daily-practice {
            background: rgba(244, 162, 97, 0.1);
            padding: 15px;
            border-radius: 10px;
            margin-top: 20px;
        }

        .practice-label {
            font-weight: bold;
            color: var(--terracotta);
            margin-bottom: 10px;
            display: block;
        }

        /* Values Section */
        .values-section {
            background: rgba(255, 255, 255, 0.5);
            padding: 100px 50px;
            position: relative;
            z-index: 1;
        }

        .values-container {
            max-width: 1200px;
            margin: 0 auto;
        }

        .values-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 30px;
            margin-top: 50px;
        }

        .value-item {
            text-align: center;
            padding: 30px;
            border-radius: 15px;
            background: linear-gradient(135deg, rgba(255,255,255,0.8), rgba(255,255,255,0.4));
            transition: all 0.3s ease;
        }

        .value-item:hover {
            transform: scale(1.05);
        }

        .value-icon {
            width: 80px;
            height: 80px;
            margin: 0 auto 20px;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 36px;
        }

        .value-title {
            font-size: 1.3rem;
            color: var(--deep-sea);
            margin-bottom: 10px;
        }

        .value-description {
            color: var(--olive);
            line-height: 1.5;
        }

        /* Call to Action */
        .cta-section {
            padding: 100px 50px;
            text-align: center;
            background: linear-gradient(135deg, var(--mist), var(--starlight));
            position: relative;
            z-index: 1;
        }

        .cta-content {
            max-width: 800px;
            margin: 0 auto;
        }

        .cta-title {
            font-size: 2.5rem;
            color: var(--deep-sea);
            margin-bottom: 20px;
        }

        .cta-text {
            font-size: 1.2rem;
            color: var(--olive);
            margin-bottom: 40px;
            line-height: 1.8;
        }

        .cta-buttons {
            display: flex;
            gap: 20px;
            justify-content: center;
            flex-wrap: wrap;
        }

        .btn-secondary {
            display: inline-block;
            padding: 15px 40px;
            background: transparent;
            color: var(--deep-sea);
            text-decoration: none;
            border-radius: 30px;
            font-size: 1.1rem;
            transition: all 0.3s ease;
            border: 2px solid var(--deep-sea);
        }

        .btn-secondary:hover {
            background: var(--deep-sea);
            color: white;
            transform: translateY(-3px);
        }

        /* Footer */
        footer {
            background: var(--star-black);
            color: var(--silver);
            padding: 40px 50px;
            text-align: center;
            position: relative;
            z-index: 1;
        }

        .footer-content {
            max-width: 1200px;
            margin: 0 auto;
        }

        .social-links {
            display: flex;
            gap: 30px;
            justify-content: center;
            margin-bottom: 20px;
        }

        .social-links a {
            color: var(--silver);
            text-decoration: none;
            transition: color 0.3s ease;
        }

        .social-links a:hover {
            color: var(--gold);
        }

        /* Responsive */
        @media (max-width: 768px) {
            nav {
                padding: 15px 20px;
            }

            .nav-links {
                display: none;
            }

            .philosophy-section,
            .values-section,
            .cta-section {
                padding: 60px 20px;
            }

            .philosophy-grid {
                grid-template-columns: 1fr;
            }

            h1 {
                font-size: 2.5rem;
            }

            .tagline {
                font-size: 1.2rem;
            }
        }
    </style>
</head>
<body>
    <!-- Cosmic Orbs -->
    <div class="cosmic-orb orb1"></div>
    <div class="cosmic-orb orb2"></div>
    <div class="cosmic-orb orb3"></div>

    <!-- Navigation -->
    <nav id="navbar">
        <div class="nav-container">
            <div class="logo">Conscious Living</div>
            <ul class="nav-links">
                <li><a href="#philosophy">Philosophy</a></li>
                <li><a href="#values">Values</a></li>
                <li><a href="#connect">Connect</a></li>
            </ul>
        </div>
    </nav>

    <!-- Hero Section -->
    <section class="hero">
        <div class="hero-content">
            <h1>Bridge Ancient Wisdom with Modern Living</h1>
            <p class="tagline">Where philosophy meets daily practice, and consciousness shapes action</p>
            <a href="#philosophy" class="cta-button">Begin Your Journey</a>
        </div>
    </section>

    <!-- Philosophy Cards Section -->
    <section id="philosophy" class="philosophy-section">
        <h2 class="section-title">Philosophy in Practice</h2>
        <p class="section-subtitle">Transform timeless wisdom into tangible daily rituals that nurture your consciousness and deepen your connection to life</p>
        
        <div class="philosophy-grid">
            <div class="philosophy-card">
                <div class="card-icon icon-presence">🌱</div>
                <h3 class="card-title">Embodied Presence</h3>
                <div class="card-philosophy">
                    "The body is your first home. Honor it as the sacred vessel of consciousness."
                </div>
                <p class="card-practice">
                    Drawing from phenomenology and somatic wisdom, we explore how physical awareness grounds us in the present moment. Your body holds intuitive knowledge that transcends intellectual understanding.
                </p>
                <div class="daily-practice">
                    <span class="practice-label">Daily Practice:</span>
                    Begin each morning with a 5-minute body scan. Notice where you hold tension, breathe into those spaces, and set an intention for embodied awareness throughout your day.
                </div>
            </div>

            <div class="philosophy-card">
                <div class="card-icon icon-flow">🌊</div>
                <h3 class="card-title">Radical Acceptance</h3>
                <div class="card-philosophy">
                    "What we resist persists. What we accept transforms."
                </div>
                <p class="card-practice">
                    Inspired by Stoic philosophy and Buddhist teachings, we learn to embrace life's impermanence. Accepting reality doesn't mean resignation—it means seeing clearly so we can respond wisely.
                </p>
                <div class="daily-practice">
                    <span class="practice-label">Daily Practice:</span>
                    When facing difficulty today, pause and say: "This is what's here now." Notice how acceptance creates space for creative response rather than reactive struggle.
                </div>
            </div>

            <div class="philosophy-card">
                <div class="card-icon icon-connection">✨</div>
                <h3 class="card-title">Interconnected Being</h3>
                <div class="card-philosophy">
                    "You are not a drop in the ocean. You are the entire ocean in a drop."
                </div>
                <p class="card-practice">
                    Through systems thinking and ecological philosophy, we recognize our fundamental interconnection. Every action ripples outward, every choice shapes the collective consciousness we share.
                </p>
                <div class="daily-practice">
                    <span class="practice-label">Daily Practice:</span>
                    Before each meal, trace one ingredient back to its source. Feel gratitude for the web of beings—farmers, transporters, earth, sun—that brought this nourishment to you.
                </div>
            </div>
        </div>
    </section>

    <!-- Values Section -->
    <section id="values" class="values-section">
        <div class="values-container">
            <h2 class="section-title">Core Values</h2>
            <p class="section-subtitle">The principles that guide our exploration of conscious living</p>
            
            <div class="values-grid">
                <div class="value-item">
                    <div class="value-icon" style="background: linear-gradient(135deg, #C36A2D, #E1C699);">
                        🌍
                    </div>
                    <h3 class="value-title">Grounded Wisdom</h3>
                    <p class="value-description">
                        Rooting philosophy in lived experience and cultural heritage
                    </p>
                </div>

                <div class="value-item">
                    <div class="value-icon" style="background: linear-gradient(135deg, #2A9D8F, #B5E2E0);">
                        💫
                    </div>
                    <h3 class="value-title">Conscious Innovation</h3>
                    <p class="value-description">
                        Bridging ancient wisdom with modern tools for transformation
                    </p>
                </div>

                <div class="value-item">
                    <div class="value-icon" style="background: linear-gradient(135deg, #F4A261, #E76F51);">
                        🤝
                    </div>
                    <h3 class="value-title">Collective Agency</h3>
                    <p class="value-description">
                        Empowering communities to shape their shared reality
                    </p>
                </div>

                <div class="value-item">
                    <div class="value-icon" style="background: linear-gradient(135deg, #5E548E, #6A4C93);">
                        🎨
                    </div>
                    <h3 class="value-title">Creative Expression</h3>
                    <p class="value-description">
                        Honoring the arts as pathways to truth and transformation
                    </p>
                </div>
            </div>
        </div>
    </section>

    <!-- Call to Action Section -->
    <section id="connect" class="cta-section">
        <div class="cta-content">
            <h2 class="cta-title">Join the Conscious Living Movement</h2>
            <p class="cta-text">
                Philosophy isn't meant to live in books—it's meant to breathe through our daily actions. 
                Together, we're building a community that bridges deep wisdom with practical living, 
                creating ripples of consciousness that transform our world from the inside out.
            </p>
            <div class="cta-buttons">
                <a href="#" class="cta-button">Start Your Practice</a>
                <a href="#" class="btn-secondary">Join Our Community</a>
            </div>
        </div>
    </section>

    <!-- Footer -->
    <footer>
        <div class="footer-content">
            <div class="social-links">
                <a href="#">Instagram</a>
                <a href="#">Medium</a>
                <a href="#">YouTube</a>
                <a href="#">Newsletter</a>
            </div>
            <p>&copy; 2025 Conscious Living. Bridging wisdom and action.</p>
        </div>
    </footer>

    <script>
        // Navbar scroll effect
        window.addEventListener('scroll', function() {
            const navbar = document.getElementById('navbar');
            if (window.scrollY > 50) {
                navbar.classList.add('scrolled');
            } else {
                navbar.classList.remove('scrolled');
            }
        });

        // Smooth scrolling for anchor links
        document.querySelectorAll('a[href^="#"]').forEach(anchor => {
            anchor.addEventListener('click', function (e) {
                e.preventDefault();
                const target = document.querySelector(this.getAttribute('href'));
                if (target) {
                    target.scrollIntoView({
                        behavior: 'smooth',
                        block: 'start'
                    });
                }
            });
        });

        // Intersection Observer for fade-in animations
        const observerOptions = {
            threshold: 0.1,
            rootMargin: '0px 0px -50px 0px'
        };

        const observer = new IntersectionObserver(function(entries) {
            entries.forEach(entry => {
                if (entry.isIntersecting) {
                    entry.target.style.opacity = '1';
                    entry.target.style.transform = 'translateY(0)';
                }
            });
        }, observerOptions);

        // Observe all cards and value items
        document.querySelectorAll('.philosophy-card, .value-item').forEach(el => {
            el.style.opacity = '0';
            el.style.transform = 'translateY(20px)';
            el.style.transition = 'all 0.6s ease';
            observer.observe(el);
        });
    </script>
</body>
</html>
